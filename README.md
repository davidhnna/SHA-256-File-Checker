# SHA-256 File Checker

A simple Python tool for calculating and verifying SHA-256 checksums for files. Useful for confirming file integrity and detecting corruption or tampering.

## Features

- Computes SHA-256 hash of any file
- Verifies file integrity by comparing hashes
- Lightweight with no external dependencies
- Easy to modify and extend
